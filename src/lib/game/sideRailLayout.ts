/** Width of the left journey rail column (rem). */
export const JOURNEY_SIDE_RAIL_WIDTH_REM = 7.5;

/** Gap between side rail and bench float (rem). @deprecated Grid shell handles spacing. */
export const JOURNEY_SIDE_RAIL_BENCH_GAP_REM = 0.5;

/** CSS custom property name shared by grid shell and layout helpers. */
export const JOURNEY_RAIL_WIDTH_VAR = "--kepi-rail-w";
